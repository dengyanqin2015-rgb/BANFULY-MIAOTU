# Vaelo 分站阶段验收（2026-09-04）

结论：不满足全功能适配验收。基础出图和素材存储通过；解析仍依赖官方 Key；谷歌上游出现 503。不得把接口成功等同于全站通过。

## 环境与变更

- 仅测试 https://banfuly-miaotu-vaelo-staging.up.railway.app/
- 业务提交：dc4b2beaf7f95b698245c74ae52bc2be970e5fed。
- 分站新增独立 Railway Volume，挂载 /data；未使用正式站卷。
- 数据库 /data/db.json，素材 /data/asset-objects，日志和模板也使用 /data。
- 用户确认测试数据无需保留；迁移临时路径后测试账号点数重新初始化。
- 密钥未写入源码或此文档。

## 已验证

- 存储申请、上传 HTTP 204、完成、读回 HTTP 200、SHA-256 一致、匿名读图 HTTP 401。
- 品牌基座页面显示“私有图片库已连接”。
- 页面 Gemini 3.1 文生图生成 1024×1024 咖啡杯，节点成功回填；服务器耗时 25.946 秒。
- 双击生成图后出现参考图缩略图。
- 带参考图的页面请求到达 Vaelo，返回上游 503；节点显示错误和诊断编号，点数没有继续减少。诊断编号 vaelo-1788513254579-ldwzt1。

## 本次脚本采样

范围：仅脚本已输出的九次请求，不混入其他用户或页面请求。1K / 1:1；时间从测试客户端发出请求至响应解析结束。失败不混入成功均值。

| 模型 | 成功/总数 | 成功平均秒 | 中位数秒 | 最慢成功秒 | 失败 |
|---|---:|---:|---:|---:|---|
| GPT | 4/4 | 30.5 | 28.3 | 38.5 | 无 |
| Gemini 3.1 | 2/3 | 23.8 | 23.8 | 24.8 | 1 次上游 503 |
| Gemini Pro | 1/2 | 30.2 | 30.2 | 30.2 | 1 次上游 503 |

原始成功秒数：GPT 29.125 / 26.979 / 27.486 / 38.520；Gemini 3.1 22.813 / 24.839；Gemini Pro 30.208。
失败客户端耗时：Gemini 3.1 1.564 秒；Pro 0.411 秒。

限制：样本很小，不代表长期 SLA。GPT 的 38.520 秒样本原标记 reference-edit，但 referenceUsed=false，实际请求仍是文生图，已纠正分类，不计入参考图通过。脚本已增加缺少参考图时禁止执行编辑测试的保护，以及 HTTPS 结果图下载。

## 确认问题与未完成项

1. 页面一键解析提示“请先配置 Gemini API Key”；src/lib/gemini.ts 的 analyzeImageForPrompt 仍调用官方 GoogleGenAI，与 Vaelo 生图路由不共用。文案排版解析也存在同类源码依赖，尚未独立实测。
2. 参数面板保留 GPT LINKAI 和 Flash 2.5 旧入口，展示与三模型中转范围不一致。
3. 谷歌上游 503 已在脚本和真实页面复现，不能认定稳定可用。
4. 下载事件等待时浏览器工具超时，结果未知，不能认定下载功能失败或通过。
5. 参考图成功出图、遮罩、多节点并发、取消重试、刷新后项目恢复、AI 助理与全案策划完整链路尚未通过验收。

下一阶段：补齐文本/视觉解析的服务端中转配置与统一模型入口，再做参考图、并发、取消和持久化验收。不要让用户自行填写官方 Key 来掩盖“全量中转”缺口。

## 2026-09-06 文本链路审计补充

- Vaelo `/api/pricing` 显示：`gemini-3.6-flash` 与 `gemini-3.1-pro-preview` 属于独立 `gemeni` 权限组，并且只声明 OpenAI 兼容端点；两个 `nanobanana` 生图模型属于另一权限组。
- 已在代码中增加统一服务端文本网关，将 AI 助理、一键解析、文案排版解析、风格/商品分析、提示词重写和详情分镜的浏览器直连统一收口。
- 网关固定映射普通与深度文本模型，兼容文字、历史消息、内嵌图片、JSON 输出和 JSON Schema；Key 不进入浏览器或请求日志。
- 当前仅代码完成，尚未部署和激活。阻塞项是测试分站缺少 `gemeni` 权限组的文本专用 Key；现有两个生图 Key 不得跨权限组复用。
